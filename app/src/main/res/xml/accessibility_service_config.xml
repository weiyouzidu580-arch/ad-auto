<?xml version="1.0" encoding="utf-8"?>
<!--
  flagReportViewIds：上报 view 资源 id（识别直播广告渲染视图 ttlive_player_render_view 需要）。
  flagIncludeNotImportantViews：**必须**带上——广告 SDK 会把广告浮层（含直播广告的渲染视图）
  标记成“对无障碍不重要”，不带这个 flag 我们的节点树里根本看不到它们
  （uiautomator 之所以能看到，是因为它默认带了这个 flag）。
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_desc"
    android:notificationTimeout="100"
    android:settingsActivity="com.wisight.adauto.MainActivity" />
